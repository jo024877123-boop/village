# 📘 평화로운 게임마을 - 관리자 & 기능 가이드 (v2.0)

이 문서는 **v2.0 업데이트**로 추가된 **디자인 리뉴얼** 및 **관리자 기능**에 대한 사용법과 Firebase 설정 관련 안내입니다.

---

## 🛠️ Firebase 설정 변경 필요 사항

**결론부터 말씀드리면, 현재 단계에서 Firebase 콘솔에서 추가로 설정할 것은 없습니다.**

새로 추가된 기능들(로고 업로드, 텍스트 편집, 연락처 링크)은 기존에 사용하던 **Firestore Database**의 `siteData`를 그대로 사용합니다.
이미지 파일 또한 별도의 스토리지(Storage) 설정 없이, 데이터베이스 내부에 텍스트 형태(Base64)로 저장되도록 구현되어 있습니다.

### ⚠️ 주의사항 (이미지 용량)
- **제한:** 이미지를 데이터베이스 문서 안에 직접 저장하므로, **너무 큰 이미지(1MB 이상)는 저장이 불가능**하거나 사이트 속도를 느리게 할 수 있습니다.
- **권장:** 로고 파일 업로드 시 **500KB 이하의 PNG/JPG 파일**을 사용해 주세요. (관리자 페이지에서 자동 체크합니다.)
- **추후 확장:** 만약 고화질 사진을 대량으로 업로드해야 한다면, 그때 **Firebase Storage** 기능을 활성화하고 코드를 수정해야 합니다. (현재는 불필요)

---

## 🎨 관리자 페이지 신규 기능 사용법

`/admin` 경로로 접속하여 로그인 후 이용할 수 있습니다.

### 1. 로고 관리 (Logo Manager)
이제 코드를 수정하지 않고 웹사이트의 로고를 변경할 수 있습니다.
- **메인 로고:** 네비게이션 바(상단)에 표시되는 로고입니다.
- **사용법:**
  1. `파일 선택` 버튼을 눌러 컴퓨터에 있는 이미지를 업로드합니다.
  2. 또는 이미지 주소(URL)가 있다면 직접 입력할 수도 있습니다.
  3. 미리보기 화면에서 로고가 잘 보이는지 확인하고 `저장하기`를 누릅니다.

### 2. 전역 텍스트 편집 (Global Text Editor)
웹사이트 곳곳에 있는 주요 문구들을 관리자 페이지에서 바로 수정할 수 있습니다.
- **수정 가능 항목:** 메뉴 이름(GNB), 메인 배너 버튼 텍스트, 섹션 타이틀 등
- **사용법:**
  1. `텍스트 편집` 탭으로 이동합니다.
  2. 원하는 항목의 텍스트를 입력합니다. (예: `navTitle` → 사이트 이름 변경)
  3. 저장하면 즉시 웹사이트에 반영됩니다.

### 3. 하단 연락처 & 푸터 (Footer & Contact)
웹사이트 하단(Footer)에 **카카오톡/디스코드로 연결되는 버튼**을 띄울 수 있습니다.
- **기능:**
  - **카카오톡 링크:** 오픈채팅방 링크 등을 입력하고 `노출` 체크박스를 켜면 사이트 하단에 카카오톡 버튼이 생깁니다.
  - **디스코드 링크:** 디스코드 초대 링크를 입력하고 `노출` 체크박스를 켜면 버튼이 생깁니다.
  - **저작권 문구:** Footer 맨 아래 `Copyright` 문구도 수정 가능합니다.

---

## 🚀 사이트 디자인 리뉴얼 (Blue Theme)

v2.0 업데이트로 사이트 전체 테마가 **'Sky Blue & Premium White'** 컨셉으로 변경되었습니다.
- **특징:** 더 밝고 화사한 색감, 부드러운 글래스모피즘(유리 효과), 고급스러운 애니메이션이 적용되었습니다.
- **관리자:** 관리자 페이지 역시 새로운 디자인 톤에 맞춰 깔끔하게 정돈되었습니다.

---

## ❓ 자주 묻는 질문

**Q. 이미지를 올렸는데 엑박이 떠요.**
A. 이미지 파일 경로가 올바르지 않거나, 이미지 파일이 깨진 경우일 수 있습니다. 다른 이미지를 다시 업로드해 보세요.

**Q. 저장했는데 사이트에서 안 바뀌어요.**
A. 새로고침(F5)을 한 번 해보세요. 그래도 안 된다면 관리자 페이지에서 `저장하기` 버튼을 눌렀을 때 "저장되었습니다" 메시지가 떴는지 확인해 주세요.
